<div class="text-green-600 font-bold text-2xl"><%= notice %></div>
<h1 class="text-4xl text-center font-bold">ノート一覧</h1>

<div class="container mx-auto px-4 mt-8">
  <% if @notes.present? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 pt-8 pb-8">
      <%= render @notes %>
    </div>
  <% else %>
    <div class="mb-3 text-center">ノートがありません</div>
  <% end %>
</div>
