<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-center mb-6">タグを編集する</h1>
  <p class="text-gray-600 text-center mb-6"><strong><%= @team.name %></strong> のタグ</p>

  <%= render 'form', team: @team, tag: @tag %>

  <%= link_to "戻る", team_tags_path(@team), class: "block text-center mt-4 text-blue-500 hover:text-blue-600" %>
</div>
