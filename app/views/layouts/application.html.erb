<!DOCTYPE html>
<html>
  <head>
    <!-- レスポンシブ対応 -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width">

    <title>見習いノート</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  </head>
  <body class="pt-20 lg:pt-0">
    <%= render 'shared/header' %>
    <%= yield %>
    <%= render 'shared/footer' %>

    <!-- フローティングアクションボタン（モバイルのみ） -->
    <% if user_signed_in? %>
      <%= link_to new_note_path,
          class: "lg:hidden fixed bottom-6 right-6 z-50 bg-green-500 hover:bg-green-600 text-white rounded-full w-14 h-14 flex items-center justify-center shadow-lg transition" do %>
        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
        </svg>
      <% end %>
    <% end %>
  </body>
</html>